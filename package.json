{
  "name": "inventory-manager",
  "private": true,
  "workspaces": [
    "front",
    "back"
  ],
  "scripts": {
    "deploy:back": "npm run test --prefix back && cd back && fly deploy",
    "deploy:front": "npm run test --prefix front && cd front && fly deploy",
    "deploy": "./deploy.sh",
    "test": "npm run test --prefix back && npm run test --prefix front",
    "test:back": "npm run test --prefix back",
    "test:front": "npm run test --prefix front",
    "test:e2e": "npm run test:e2e --prefix front",
    "test:e2e:ui": "npm run test:e2e:ui --prefix front"
  },
  "dependencies": {
    "class-transformer": "^0.5.1",
    "class-validator": "^0.14.4",
    "headroom-ai": "^0.22.4"
  }
}
